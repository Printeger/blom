# RESULT_PHASE1

本文档记录 2026-03-24 对 `phase_1/` 代码的重新运行结果。  
结构统一为“步骤 -> 功能说明 -> 实际结果 -> 分析”，所有数值均来自本次实际执行。

本次 Phase 1 复核运行的目标有两点：

- 纠正之前结果文档命名和归档上的混淆；
- 在 Phase 2 完成后，再次确认 Phase 1 baseline 仍然可复现、可运行、可验证。

主验证案例使用：

- `q = [0.0, 0.2437736638035451, -0.5882136211888513, 0.012147335456314523, 0.7645991085692857, -0.7962290423537836]`
- `T = [1.261139701990353, 1.2860643052769538, 0.6281136326755459, 0.9503859378955671, 0.8707980242325812]`
- `zeta_start = [0.0, 0.0, 0.0, 0.0]`
- `zeta_end = [-0.7962290423537836, 0.0, 0.0, 0.0]`

这对应一个 `M = 5` 的标量 minimum-snap Phase 1 母问题。

## Step 1. 静态可执行性检查

### 运行命令

```bash
python3 -m compileall phase_1
```

### 功能说明

确认 `phase_1/` 目录下的模块和示例脚本可以被 Python 正常解析、导入和编译。

### 实际结果

```text
Listing 'phase_1'...
Listing 'phase_1/examples'...
Compiling 'phase_1/examples/demo_case_random.py'...
Compiling 'phase_1/examples/demo_case_two_segment.py'...
Compiling 'phase_1/minco_scalar_baseline.py'...
Compiling 'phase_1/test_minco_scalar_baseline.py'...
```

### 分析

- `compileall` 正常结束，说明当前 Phase 1 代码在语法和模块导入层面没有问题。
- 这是后续所有数值验证的前置条件。

## Step 2. 构造 Phase 1 线性系统并求解全局系数

### 运行方式

通过 Python 调用：

```python
M_mat = build_system_matrix(T)
b_vec = build_rhs(q, zeta_start, zeta_end)
result = solve_minco_coefficients(q, T, zeta_start, zeta_end, return_system=True)
```

### 功能说明

把问题写成

```text
M(T) c = b(q, zeta_start, zeta_end)
```

并求出全局 7 次多项式系数：

- `coeffs`: 每段 8 个系数
- `c_vec`: 全局展平系数
- `M(T)`: 只依赖时间分配的系统矩阵
- `b(...)`: 由 waypoints 和边界 jet 构成的右端项

### 实际结果

- `M_seg = 5`
- `T_total = 4.9965016020710005`
- `M_mat.shape = (40, 40)`
- `b_vec.shape = (40,)`
- `rank(M_mat) = 40`
- `cond(M_mat) = 107403.08584332098`
- `coeffs.shape = (5, 8)`
- `c_vec.shape = (40,)`
- `residual_norm = 1.4884608173849024e-13`

第 0 段系数：

```text
[0.0, 4.9805614400150457e-14, 9.860761315262648e-32, 2.3475549359530015e-16,
 0.3784892576633074, -0.24316335455691698, -0.005641597167162911,
 0.016708852080439564]
```

第 2 段系数：

```text
[-0.5882136211888513, -0.7355083660028487, 1.8667147464905742, 2.094201098697996,
 -0.3978043798272684, -1.2230682514141666, -0.5294261799126017,
 0.5424049961950078]
```

第 4 段系数：

```text
[0.7645991085692857, -2.9763095568684568, -2.5691577811970654, 5.647354684007154,
 3.0813414327880495, -4.2117545406000465, -3.7800063611553965,
 3.2535038254180946]
```

### 分析

- 每段 8 个系数、共 5 段，因此总未知量为 `40`，系统尺寸正确。
- `rank(M_mat) = 40` 说明系统满秩，Phase 1 中固定初末端 boundary jet 的唯一性假设在数值上成立。
- 条件数约 `1.07e5`，对高阶多项式系统来说仍处于可稳定求解范围。
- 系统残差在 `1e-13` 量级，说明解算准确。

## Step 3. 插值、连续性、边界 jet 和系统残差验证

### 运行方式

```python
interp = interpolation_errors(result["coeffs"], T, q)
jumps = continuity_jumps(result["coeffs"], T)
bc = boundary_jet_errors(result["coeffs"], T, zeta_start, zeta_end)
residual = system_residual(result["M"], result["c_vec"], result["b"])
```

### 功能说明

检查 Phase 1 最关键的四类性质：

- waypoint 插值是否满足
- 相邻段连接处是否连续
- 起点和终点 jet 是否满足
- 线性系统残差是否足够小

### 实际结果

- `max_interpolation_error = 5.252742685257772e-15`
- `max_continuity_jump = 7.815970093361102e-14`
- `max_boundary_jet_error = 4.9805614400150457e-14`
- `system_residual = 1.4884608173849024e-13`

按当前 helper 默认连续性统计列得到的最大 jump：

- 第 1 列：`7.216449660063518e-16`
- 第 2 列：`5.684341886080802e-14`
- 第 3 列：`7.815970093361102e-14`
- 第 4 列：`5.684341886080802e-14`

### 分析

- 所有误差都在 `1e-13` 到 `1e-15` 量级，明显优于常见的 `1e-8` 验收尺度。
- 插值误差基本处于机器精度范围内，说明 waypoints 被准确命中。
- 边界 jet 误差同样很小，表明 Phase 1 确实实现了“固定初末端 boundary jet”的增强版母问题，而不是退回到自然边界近似。

## Step 4. 轨迹采样与导数接口验证

### 运行方式

```python
samples = sample_trajectory(result["coeffs"], T, num_per_segment=6, orders=(0, 1, 2, 3, 4))
```

### 功能说明

验证求解后的多项式系数能否稳定地转成可采样的全局轨迹，并输出位置、速度和 snap 等导数信息。

### 实际结果

前 10 个全局采样时刻：

```text
[0.0, 0.2101899503317255, 0.420379900663451, 0.6305698509951765,
 0.840759801326902, 1.0509497516586275, 1.261139701990353,
 1.4754837528698452, 1.6898278037493375, 1.9041718546288298]
```

前 10 个位置样本：

```text
[0.0, 0.0006388125911881221, 0.008635404124481352, 0.035905207533053096,
 0.0899363661396167, 0.16603072401396204, 0.2437736638035451,
 0.2895655393708832, 0.26150950380691973, 0.13220196371337192]
```

前 10 个速度样本：

```text
[4.9805614400150457e-14, 0.011681936314379323, 0.07470221674034308,
 0.19134562077861844, 0.31934369292949993, 0.3883629752505025,
 0.3237551160360915, 0.07077572981675027, -0.35545029341775314,
 -0.8485733262453099]
```

前 10 个 4 阶导数样本：

```text
[9.083742183919377, 2.99109008067546, -2.499006984559153, -6.604537775091849,
 -8.543491054230001, -7.533855585281007, -2.7936201315522453,
 5.937832029272141, 15.661069571433313, 22.67513356146627]
```

### 分析

- 轨迹在全局时间轴上连续可采样，接口已经可直接服务于后续验证和可视化。
- 位置样本从 `0.0` 平滑推进到第一个 waypoint，速度和高阶导数都呈现连续演化，没有明显数值爆炸。
- 这说明系数组织方式、全局时间定位和单段求值函数之间是互相一致的。

## Step 5. 单段 demo

### 运行命令

```bash
python3 -m phase_1.examples.demo_case_single_segment
```

### 功能说明

验证 `M = 1` 的最小可解场景，确认单段情况下的母问题构造和边界约束仍然成立。

### 实际结果

```text
Single-segment Phase 1 demo
coeffs shape: (1, 8)
coefficients: [ 0.00000000e+00  2.36847579e-14  0.00000000e+00 -1.26318709e-14
  6.91358025e+00 -1.10617284e+01  6.14540466e+00 -1.17055327e+00]
system residual: 9.875e-14
```

### 分析

- 单段问题可以正常求解，说明 Phase 1 的实现没有把 `M >= 2` 当成隐藏前提。
- 残差仍在 `1e-13` 到 `1e-14` 量级，说明边界型约束单独就足以稳定确定一段 7 次多项式。

## Step 6. 两段对称 demo

### 运行命令

```bash
python3 -m phase_1.examples.demo_case_two_segment
```

### 功能说明

用一个时间和 waypoint 对称的案例，检查求解结果是否保留应有的对称性。

### 实际结果

```text
Two-segment symmetric Phase 1 demo
coeffs shape: (2, 8)
max symmetry error: 3.275e-15
system residual: 3.762e-13
```

### 分析

- 对称误差只有 `3.275e-15`，基本就是数值舍入级别。
- 这类 sanity check 很重要，因为它能从另一个角度证明系统装配没有行顺序或符号上的系统性偏差。

## Step 7. 随机案例 demo

### 运行命令

```bash
python3 -m phase_1.examples.demo_case_random
```

### 功能说明

在更一般的随机 waypoint 与时间分配下，验证整体求解流程的健壮性。

### 实际结果

```text
Random Phase 1 demo
q: [ 0.          0.24377366 -0.83198728  0.60036096  0.75245177 -1.56082815]
T: [1.47562235 1.2611397  1.28606431 0.62811363 0.95038594]
system residual: 1.084e-13
max interpolation error: 1.232e-14
max continuity jump: 6.484e-14
max boundary jet error: 1.625e-14
```

### 分析

- 在随机场景下，残差和误差仍维持在 `1e-13` 到 `1e-14` 水平，说明实现并不依赖人为构造的平滑样例。
- 这一步很好地支撑了“Phase 1 已可作为 Phase 2 固定后端”的结论。

## Step 8. 单元测试

### 运行命令

```bash
python3 -m unittest phase_1.test_minco_scalar_baseline
```

### 功能说明

对 Phase 1 的核心函数做自动化回归验证，避免只依赖手工 demo。

### 实际结果

```text
.......
----------------------------------------------------------------------
Ran 7 tests in 0.014s

OK
```

### 分析

- `7` 个测试全部通过，说明系统构造、求解、评估和误差检查接口目前保持一致。
- 结合上面的 demo 和主案例数值，可以认为当前 Phase 1 baseline 是稳定可复用的。

## 总结

本次重新运行表明：

- Phase 1 代码能够正常编译、执行和通过单元测试；
- `M(T)c=b` 的系统构造是满秩且可稳定求解的；
- 插值、连续性、边界 jet 和残差都处于机器精度附近；
- 单段、对称、随机三类 demo 都表现稳定。

因此，`phase_1/minco_scalar_baseline.py` 目前可以继续作为 Phase 2 验证代码的固定求解后端。
